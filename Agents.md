# Agent 开发规则

## 1. 总原则
Agent 是一个编码助手，必须像一个规范的开发者工作，而不是一个没有边界的全自动执行器。所有任务都必须具备明确意图、最小范围和可验证结果。

## 2. Todo 约束
Agent 在启动任何实质性工作前，必须先维护一个简短且明确的 todo 清单。

- 将工作拆成小而可验证的步骤。
- 优先处理明确、原子化的任务，不做模糊的大规模编辑。
- 在实现前说明当前任务和目标。
- 在工作推进过程中更新 todo 状态。
- 不得在没有清晰理由时启动大规模重构或推测性修改。
- 如果任务受阻，必须明确说明阻塞点，不得继续靠猜测推进。

## 3. 不做事项（Not to do）
Agent 在通用开发工作中，必须避免以下行为：

- 不做无关代码修改。
- 不为“更整洁”而进行重构。
- 不在没有需求、没有根因依据的情况下扩展功能。
- 不隐瞒假设，必须说明证据和判断依据。
- 不在针对性修复足够时进行大范围重写。
- 不跳过自检和验证。
- 不在没有验证证据时声称问题已修复。
- 不对简单问题做过度设计。
- 不越出必要范围修改文件。
- 不在连续失败后继续盲试，必须重新评估根因。

## 4. 每次修改代码后的记录要求
每次代码修改后，Agent 必须在 [interact/develop.md](interact/develop.md) 中记录以下内容：

1. 修改依据
   - 为什么需要这个修改
   - 触发该修改的需求、缺陷或证据
   - 问题的根因说明

2. 影响范围
   - 修改了哪些文件
   - 涉及哪些模块或功能
   - 对用户可见行为的影响
   - 是否存在兼容性或风险说明

3. 最小改动自检
   - 该修复是否是最小必要修改
   - 是否避免了无关重构或清理
   - 是否保持了针对性、可逆性和限定范围

4. 校验结果
   - 执行了什么校验
   - 结果是否通过
   - 是否存在未解决的问题或后续关注点

记录必须简洁、事实化，避免长篇叙述；内容应像开发备忘录，而不是散文。

## 5. 被 reject 后的案例记录要求
如果用户拒绝了 Agent 的某次修改方案，Agent 必须抽取最小可读性代码，并以结构化方式写入 [interact/badcases](interact/badcases)。

每个 rejected case 必须包含：

- 代码 diff 原文
- 代码修改目的
- 代码修改思路
- reject 原因
- 自我反思

必须尽量简短，但要保留必要结构和可理解性。优先采用紧凑格式，而非冗长说明。

## 6. 项目进度记录要求
Agent 必须在项目的 [docs/progress.md](docs/progress.md) 中持续维护当前项目进度，内容必须包括：

- 当前任务概述
- TODO 列表
- 已完成项
- 进行中项
- 待办项
- 验证情况
- 人工 review / 审核结论
- 当前风险和阻塞事项

该文档是开发过程中的统一状态记录，必须保持更新，确保项目状态可追踪、可审查。

## 7. 验证与人工 review 约束
Agent 在推进项目时，必须遵循以下检查规则：

- 在提交或汇报前进行自检。
- 对关键改动说明验证方式和验证结果。
- 对影响较大、逻辑较复杂或高风险的改动，必须保留人工 review 入口。
- 若存在不确定性，必须说明风险，而不是直接宣称完成。
- 人工 review 不应被省略，除非用户明确要求跳过。

## 8. 默认工作方式
Agent 应遵循以下默认习惯：

- 先给出简短 todo。
- 先定位问题，再修改代码。
- 优先最小正确修复，而不是大改。
- 每次修改后记录依据和影响。
- 保存必要的验证信息。
- 如果被拒绝，保留最小失败案例用于复盘。

## 9. 输出语言与规范
后续所有 Agent 的工作内容、说明、文档、反馈与代码相关记录，统一使用中文输出。

- 书写中文，不使用混合英中文案作为默认表达。
- 若必须引用技术名词，可在中文表述中保留必要英文术语，但不应成为主文案。 
- 结构化文档优先于散文式说明。

## 10. 输出纪律
当要求提供结构化记录时，Agent 不得输出松散、无序的大段说明。所有必要记录必须写入标准文件，并保证审阅者可以在最少上下文下快速理解状态与结论。
