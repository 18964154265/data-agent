# 项目进度记录

## 1. 当前任务

- 目标：实现通用 ReAct 数据分析 Agent，支持文档 ETL，高效处理 `public/input`，交付可复现程序、答案和过程记录。
- 范围：现有 CSV、JSON、SQLite、Markdown/TXT，及可选文本 PDF；不编写任务 ID 专属求解规则，不读取标准答案参与求解。
- 当前结论：核心开发和离线验证完成；真实模型验收等待接口配置，人工审阅待进行。

## 2. 实施计划与 TODO

- [x] P0：阅读 Agents.md、全部设计文档和代码，盘点 50 个任务。
- [x] P1：统一 DuckDB 数据目录，流式/分批加载来源，隔离只读查询与工作区。
- [x] P2：Schema 规划、实体/段落分块、语义抽取、证据校验、保守合并、质量报告、缓存和局部重试。
- [x] P3：受控目录/Schema/查询工具、SQL 程序版本校验编辑、执行、自检和提交。
- [x] P4：完整尝试重启、正式运行路径统一任务超时、模型有界重试、增量事件和性能统计。
- [x] P5a：离线回归及真实输入准备验证，提供独立评分命令。
- [ ] P5b：真实模型联通、文档质量抽查、代表性任务和全量准确率/吞吐验收。
- [x] P6a：同步使用说明、项目结构、开发备忘录、验证报告和人工审阅清单。
- [ ] P6b：人工审阅结论及必要修正。

## 3. 已完成

- 数据盘点：50 个任务，105 张可注册结构化表，14 份业务文档；大文件采用流式或分批处理。
- 程序化答案：`solution.sql` 与探查共享 DuckDB；旧版本/旧产物不能提交，结果上限不静默截断。
- ETL：字段类型、单位、复合主键、逐字段段落引用、明确身份覆盖检查、拒绝和冲突报告。
- 可靠性：每次尝试独立脚手架，默认拒绝不完整 ETL；单任务错误可隔离，串行批跑不再绕过超时。
- 可观测性：请求消息/响应、token 用量明细、步骤耗时、阶段指标、尝试记录与增量汇总。
- 流程：每批修改有 [开发记录](../interact/develop.md)；文档/测试可进入版本控制；输入、产物、本地密钥配置保持忽略。

## 4. 进行中与待办

- 进行中：等待真实模型配置与人工审阅反馈；已授权且不依赖接口的开发、自检均已完成。
- 待办：P5b、P6b。没有执行 Git 提交、合并或部署。

## 5. 验证情况

- 41 个回归测试通过，最终一次约 1.14 秒。
- Ruff、Python 编译检查、Git 差异空白检查通过。
- 50 个任务的结构化来源加载与 Schema/样本查询全部通过，0 个错误任务，总计约 15.924 秒。
- 此性能数字不包含文档模型抽取或 ReAct；脚本模型验证不能代替真实准确率。
- 详细证据与已修复问题见 [verification.md](verification.md)。

## 6. 人工审阅

- 审阅者：待指定。
- 结论：待审阅，不能视为已通过。
- 入口：[review.md](review.md)、当前差异、开发记录与测试报告。
- 重点：Schema/复合主键、原文更正值、冲突门槛、SQL 权限、重试无污染和指标含义。

## 7. 风险与阻塞

- 阻塞：没有模型接口配置，默认 `OPENAI_API_KEY` 不存在；已询问配置文件路径或环境变量名称，不需要在聊天中发送密钥。
- 准确率：尚未实测，不承诺 50 题全部正确；引用存在和程序完成不代表语义正确。
- ETL：无明确编号实体的召回率、跨块更正、低频字段 Schema 漏采仍需评估；缺少完整身份不猜测归并。
- 资源：每任务 DuckDB 配置 1 GB 上限不覆盖所有 Python/模型客户端内存；输入准备未采样峰值内存，不能声称全进程有硬内存隔离。
- 权限：正式模型工具不暴露任意 Python，但这是应用和数据库限制，不是操作系统容器。
- 调试：显式注入模型/工具的 Python 接口在当前进程执行，不具备强制任务截止时间。
- 预留设计：辅助身份语义挂接、Schema 多轮合并、数值约束修复、OCR、自动降级及完整断点续跑未实现，不标记为完成。

## 8. task_11 联通验证（本轮）

- [x] 激活 conda kdd 并检查依赖、模型配置。
- [x] 使用正式 CLI 执行 task_11（运行被缺少模型密钥阻塞）。
- [x] 核对失败 trace；未进入 SQL 求解，未生成预测文件。
- 当前状态：kdd 依赖按 uv.lock 安装完成，pip check 通过；正式 CLI 已运行，但在配置检查阶段返回 configuration_error，缺少模型 API key。
- 运行命令：`conda activate kdd` 后执行 `dabench run-task task_11 --config configs/react_baseline.example.yaml`。
- 运行目录：`artifacts/runs/20260922T103742691904Z/task_11/`，保存 trace.json 与 events.jsonl，无 prediction.csv。
- 待用户补充：实际模型配置文件路径或模型/接口/密钥环境变量信息；没有读取标准答案，尚不能宣称端到端流程打通。

## 9. task_11 配置更新后复跑

- [ ] 加载更新的配置与 .env（不输出密钥）。
- [ ] 在 conda kdd 中执行正式 CLI。
- [ ] 核对 SQL、trace 和 prediction.csv，交由用户验收。
- 当前状态：用户已完成环境配置，正在复跑。

### task_11：仅配置文件模式
- 用户要求不加载 .env；已停止之前的运行。
- .env 已不存在，当前 YAML 也没有 api_key，因此未能迁移密钥，未创建本地配置。
- 已在 configs/react_baseline.example.yaml 添加 api_key 空字段并清空 api_key_env，保留模型与接口配置。
- 阻塞：需要用户直接在配置文件中填写 api_key，才能继续 task_11。

### task_11：用户补齐 YAML 密钥后复跑
- [x] 检查 YAML api_key 已填写，不读取 .env。
- [x] 在 kdd 中运行 task_11。
- [x] 核对执行状态、SQL 和预测文件，交由用户检查。

### task_11 正式运行结果
- 状态：成功，使用 YAML api_key，未加载 .env。
- 运行目录：`artifacts/runs/20260922T112057863507Z/task_11`。
- 总耗时：89.7 秒；模型请求 8 次；完整尝试 1 次。
- 预测：3 行，3 列，SQL/trace/events 均已保存。
- 当前结论：task_11 的结构化输入→模型探查→SQL 执行→CSV 交付链路打通；未读取标准答案，答案正确性待用户检验，不据此宣称 ETL 或全量准确率通过。

## 2026-09-23：取消重复结果文件
- 当前任务：后续运行不再生成 attempts/<n>/result.csv，仅正式提交时生成 prediction.csv。
- [x] 定位生成和提交校验逻辑。
- [ ] 移除中间 CSV 写入，保留内存结果和程序版本校验。
- [ ] 验证最终提交及失败后旧结果失效，更新说明与开发记录。
- 人工审阅：用户已否定重复 CSV 设计，修正后待核对。
- 风险：需保证移除文件存在性检查后，仍不能提交未执行或过期结果。
