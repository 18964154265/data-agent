# 开发备忘录

## 2026-09-22 P1：统一数据层
- 依据：公开输入混用 CSV、records 包装 JSON 和 SQLite，最大单文件约 279 MB；现有工具无法统一关联且 Python 可改写输入。
- 范围：新增 storage.py、data/catalog.py、data/query.py；将结构化来源物化到任务工作区，查询禁用外部访问；以带版本的 SQL 文件生成答案。
- 最小改动：沿用现有包和依赖，新增独立准备/查询模块；不修改输入，不引入通用沙箱或任意代码执行器。
- 验证：代码已写入，依赖安装中；待集成测试检查格式兼容、SQL 越权、版本冲突和大表加载。
- 风险：SQLite 动态类型与超大 JSON 需要真实数据验证；引擎限制不是操作系统安全沙箱。

## 2026-09-22 P2：文档 ETL
- 依据：业务文档的字段跨章节分布，存在旧值更正、复合观测键和干扰叙述；不能直接按固定窗口回答问题。
- 范围：新增 etl/schema.py、documents.py、pipeline.py，包含跨全文 Schema 采样、身份线索分组、分块并发抽取、有界重试、逐字段引用验证、保守合并和内容寻址缓存。
- 最小改动：仅抽取为标准关系表，不添加任务专属答案规则；不推测补值、不自动解决多候选冲突；不实现无证据的算术修复。
- 验证：待离线测试核验跨段落合并、引用拒绝、复合主键、失败块隔离及缓存失效。
- 风险：Schema 采样与模型自报段落覆盖不能证明语义全覆盖，需真实模型验收；PDF 入口需要可选依赖，不提供 OCR。

## 2026-09-22 P3：受控 ReAct 与模型预算
- 依据：基线直接提交模型数据行、模型异常丢失步骤、无请求统计，无法支撑可复现分析。
- 范围：配置增加 ETL、查询、尝试与预算；模型加入超时/SDK 有界重试和逐请求记录；ReAct 保留模型失败前的步骤并压缩旧观察；新增受控 SQL 工具集。
- 最小改动：保留 ModelAdapter、ReActAgent、ToolRegistry 协议及旧工具模块作为兼容入口，新任务链路使用受控工具；未加入任务专属逻辑。
- 校验：初次 ruff 检出两处未使用导入，已删除；真实来源加载检查正在运行，完整回归待后续执行。
- 风险：历史折叠保留任务和近期观察，但需重新查询早期事实；模型重试由 SDK 处理，调用预算统计的是逻辑请求。

## 2026-09-22 P4：运行器集成与恢复
- 依据：串行批跑原先绕过任务超时；进程队列在大结果下可能阻塞；完整尝试未隔离且过程仅结束时保存。
- 范围：runner.py 接入数据准备/ETL/受控工具，每次尝试独立 SQL 和结果，默认严格拒绝不完整 ETL；spawn 子进程统一串行与并发截止时间，原子文件返回结果，保留增量事件与步骤快照；批次汇总逐任务更新。
- 最小改动：只调整任务编排必经路径，保留公开 run_single_task/run_benchmark 接口和依赖注入；旧基线工具仅在显式注入时使用。
- 校验：待测试验证 spawn 超时、串行路径、失败隔离、重试干净起点和过程保留。
- 边界：注入自定义模型/工具的离线调试路径在当前进程执行，不提供进程级强制截止；正式 CLI 始终使用受控路径。

## 2026-09-22 验证修正：大型 JSON 与实体块边界
- 依据：task_11 的真实 JSON 在 1 GB DuckDB 上限下触发预分配内存错误；提高 maximum_object_size 会扩大缓冲，即使文件很小也失败。
- 范围：JSON 加载改为 ijson 流式拆解 records 包装并转逐行 JSON，再由 DuckDB 全量推断类型；新增 ijson 依赖与可选 PDF 依赖。实体组能放进单块时提前换块，避免被前组挤断。
- 最小改动：仅替换 JSON 输入适配与块装箱逻辑；保留表名、字段和查询接口。
- 校验：task_145 的 CSV+SQLite、task_330 的 279 MB CSV 已成功加载；JSON 修正待复验；当前源码 ruff 与编译检查通过。

## 2026-09-22 P5：首批回归测试
- 依据：数据与执行边界涉及多种格式、空值和重试，需以可重复断言验证。
- 范围：新增 tests/test_data_runtime.py、test_etl.py；覆盖混合来源关联、越权 SQL、查询中断、旧结果拒绝、JSON 包装、证据冲突、复合键和缓存失效。
- 最小改动：测试使用小型合成数据，专门构造失败场景，不把公开答案写入求解代码。
- 校验：测试文件已写入，依赖更新完成后执行；尚未宣称通过。

## 2026-09-22 P5：端到端与流程记录
- 依据：需证明 ETL→查询→程序→交付链路可运行，且失败尝试、串行超时和坏任务隔离均可重复验证。
- 范围：新增 runner 回归；更新示例配置到 public/input；取消 docs/tests 忽略以便纳入代码审查，新增 public/ 忽略保护用户数据；开发记录改为中文。
- 最小改动：不提交数据、密钥或生成产物；不更改既有设计文档内容。
- 校验：首批 19 个数据/ETL 测试通过；task_11 和 task_218 真源加载通过；大 JSON 及端到端测试继续运行。

## 2026-09-22 P5：真实输入全量验证入口
- 依据：合成测试通过不能代表 50 个公开任务的数据兼容性，需要单独记录加载和查询结果。
- 范围：新增 scripts/validate_inputs.py，逐任务准备、列出 Schema/样本并记录耗时；修正 .gitignore 的 data/ 为根目录锚定，避免错误忽略新数据模块。
- 最小改动：只读 public/input，不读取 public/output；验证产物进入 artifacts/validation。
- 校验：26 个回归测试和 ruff 通过；启动 50 任务全量输入验证。

## 2026-09-22 真实数据修正：时间值序列化
- 依据：全量输入检查在 task_86 的 TIME 字段取样时失败，原 JSON 转换未覆盖 datetime.time。
- 范围：storage.json_value 补齐时间、间隔和 UUID；增加时间/日期/定点数回归。验证脚本逐任务保存报告并记录取样错误，避免后续任务被中断；修正 CLI 数据目录展示。
- 最小改动：只扩展结果序列化，不改变数据计算；ETL 缓存同时纳入提示指纹和主键完整性校验。
- 校验：26 个前序测试通过；新用例与全量输入重跑待完成。

## 2026-09-22 P5：独立答案验收入口
- 依据：公开目录包含标准答案，但运行成功不等于语义正确，需要独立、可复现且不泄漏答案的比较工具。
- 范围：新增 benchmark/evaluate.py 与 dabench evaluate；默认忽略行顺序及列名但保留重复行和列顺序，数字显示等价，舍入必须显式配置；汇总记录计划任务 ID，未完成任务计入分母。
- 最小改动：评分模块不被求解运行器引用，不读取答案用于 ETL 或提示；明确此为本地指标而非官方评分。
- 校验：新增重复行、可选行序、显式舍入、前导零和缺失任务回归，待执行。

## 2026-09-22 自检修正：遗漏实体与局部重试
- 依据：模型可以声明段落已覆盖但漏掉实体；原字段证据错误只在汇总时发现，未触发块重试。
- 范围：每块校验逐字段引用和主键，针对明确身份锚点核对抽取覆盖并返回遗漏 ID 促使重试；记录无锚点段落数；治理文件也通过上下文真实路径检查。
- 最小改动：仅对确定的单身份段落做覆盖检查，不强行归并复杂关系或无编号段落，不推测修复。
- 校验：新增虚报覆盖、部分块失败、损坏缓存和配置校验回归；全量 50 个任务的结构化来源加载/Schema 取样已通过，文档语义准确率仍未测。

## 2026-09-22 自检收敛：失败可诊断与产物合法性
- 依据：工具失败原先丢弃已解析动作，记录回调异常被错误归为工具失败；计算产生无穷值不应写出非标准 JSON。
- 范围：ReAct 保留失败工具与参数及错误类型，日志回调置于工具异常捕获外；拒绝 NaN/Infinity 结果；缺少接口密钥提前失败并记录事件，避免浪费数据准备成本。
- 最小改动：仅修正错误边界与结果校验，不扩展求解范围；新增两项回归。
- 校验：此前 39 项测试、ruff、git diff --check 通过；最终验证将在文档收敛后记录。

## 2026-09-22 P6：使用说明与审阅入口
- 依据：旧项目说明描述的是直接答案提交的基线，必须同步到当前 SQL/ETL 链路并保留历史差异。
- 范围：更新 README.zh.md、README.md 顶部提示、docs/project.md；保留 project_baseline.md 历史快照；新增 review.md，明确人工结论待审阅。
- 最小改动：保留 etl_design.md、react.md 的原始设计要求，不将未实现的高级能力写成已完成。
- 校验：链接和最终测试结果将在 verification.md 汇总；模型接口仍缺配置，未宣称已取得真实任务准确率。

## 2026-09-22 最终自检记录
- 依据：实现需要以可复现结果和明确未验收项收尾，不能将运行率当作准确率。
- 范围：汇总 verification.md、progress.md、review.md；不新增求解行为。
- 最小改动：未调整原始业务数据和设计文档，未写任务专属解法，未提交/合并代码。
- 校验：41 项测试通过（1.14 秒），Ruff 通过，git diff --check 通过；50 任务、105 张结构化表加载/取样通过（15.924 秒，0 错误）。
- 未完成：真实模型与 14 份业务文档语义验收需要接口配置；人工审阅待进行。离线结果不构成真实任务准确率证据。

## 2026-09-22 conda kdd / task_11 运行记录
- 依据：用户要求使用 conda kdd 实跑 task_11，检查流程。
- 范围：向 kdd（Python 3.12.13）安装当前项目及锁定依赖；未改业务代码、输入或模型配置。
- 环境处理：首次下载连接中断；简化锁约束混入不同 Python 版本后产生冲突，改用 uv export 正式导出的条件依赖安装成功。
- 校验：pip check 通过；正式 CLI 运行返回 configuration_error（缺少 API key），运行记录位于 artifacts/runs/20260922T103742691904Z/task_11/trace.json。
- 结论：环境和 CLI 可启动，模型求解尚未执行，无预测文件；等待实际模型配置后重新运行，不能判定端到端成功。

## 2026-09-22 task_11 配置对齐
- 依据：用户已提供 .env；其中 API_KEY 有值，但 YAML 的 api_key_env 不是环境变量名，导致解析后密钥为空。
- 范围：仅将示例配置的 api_key_env 改为 API_KEY；模型和接口保持用户设置。通过 artifacts/validation/run_configured_task.py 加载 .env 并调用正式 CLI，不回显变量值。
- 最小改动：没有改求解代码或数据，没有复制密钥进产物配置。
- 校验：随后复跑 task_11，结果待记录。

## 2026-09-22 仅配置文件模式
- 依据：用户要求不使用 .env；当前 .env 已不存在，YAML 尚无 api_key。
- 范围：示例配置增加 api_key 空字段并将 api_key_env 置空，禁用环境变量回退；停止旧运行，不改业务代码。
- 最小改动：保留现有模型、接口和运行参数，未写入或回显任何密钥。
- 校验：YAML 解析通过；因密钥尚未填写，真实运行待用户补充。尝试迁移未成功，未生成本地配置或预测文件。

## 2026-09-22 task_11 真实模型运行成功
- 依据：用户已补齐 YAML 密钥，要求直接运行。
- 范围：仅执行正式 CLI，无源码修改，不加载 .env。
- 校验：succeeded=true，3 行 × 3 列，8 次请求，耗时 89.7 秒；产物位于 artifacts/runs/20260922T112057863507Z/task_11。
- 审阅：答案由用户检验，未读取标准答案；本任务不含业务文档，不能代表文档 ETL 验收。
